
  # global dashboard

  This is a code bundle for global dashboard. The original project is available at https://www.figma.com/design/2aWLsL5DMxhiQLHttPDDsa/global-dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  